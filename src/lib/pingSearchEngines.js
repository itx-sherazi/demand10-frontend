// utils/pingSearchEngines.js
// This function has been deprecated as Google and Bing no longer support sitemap ping endpoints
export async function pingSearchEngines(sitemapUrl) {
  // No-op function - Google and Bing ping endpoints are deprecated
  // Keeping this function to avoid breaking existing imports
  return Promise.resolve();
}
