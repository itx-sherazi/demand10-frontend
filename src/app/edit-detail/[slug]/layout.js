export const metadata = {
  title: 'Edit Company | IntentWire',
  description: 'Edit your company information',
};

export default function EditCompanyLayout({ children }) {
  return (
    <div className="min-h-screen bg-gray-50">
      {children}
    </div>
  );
}